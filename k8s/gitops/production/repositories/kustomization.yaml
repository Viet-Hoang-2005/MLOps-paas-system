apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - addons.yaml

patches:
  - target:
      version: v1
      kind: Secret
    patch: |-
      - op: add
        path: /metadata/annotations
        value:
          argocd.argoproj.io/sync-wave: "-100"
