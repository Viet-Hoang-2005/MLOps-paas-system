apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: postgres-bootstrap-secret-sync
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  refreshInterval: "1h"
  secretStoreRef: {name: aws-secrets-manager, kind: ClusterSecretStore}
  target:
    name: postgres-bootstrap-secret
    creationPolicy: Owner
    deletionPolicy: Retain
  data:
    - {secretKey: DB_USER, remoteRef: {key: mlops/production-secrets, property: DB_USER}}
    - {secretKey: DB_PASSWORD, remoteRef: {key: mlops/production-secrets, property: DB_PASSWORD}}
    - {secretKey: username, remoteRef: {key: mlops/production-secrets, property: DB_USER}}
    - {secretKey: password, remoteRef: {key: mlops/production-secrets, property: DB_PASSWORD}}
