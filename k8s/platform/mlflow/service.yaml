apiVersion: v1
kind: Service
metadata:
  name: mlflow-server
  namespace: mlflow-server
  labels:
    app: mlflow-server
spec:
  type: ClusterIP
  selector:
    app: mlflow-server
  ports:
    - name: http
      port: 5000
      targetPort: 5000
